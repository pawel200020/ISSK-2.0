@page "/"
@using System.Globalization
@using PortalBlazor.Components.Controls
@using Resources.PortalResources

<PageTitle>Home</PageTitle>
@PortalResources.Title
<h1>Hello, world!</h1>

Welcome to your new app

<CountryFlag Country="Country.DE" Size="FlagSize.Normal"></CountryFlag>

@code {
    [Parameter] public string? CSSClass { get; set; }
    [Parameter] public string Width { get; set; } = "200px";

    public void OnSelectedValueChange(string selectedValue)
    {
        var t = selectedValue;
    }

    public void OnInitialized()
    {
        
    }
}