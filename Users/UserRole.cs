namespace Users;
/// <summary>
/// This enum encapsulates roles stored in database every edit needs a migration
/// </summary>
public enum UserRole
{
    Administrator,
    Manager,
    Standard,
    Basic
};